namespace Utility.Helpers
{
    public class FilesManager
    {
        private void CreateIfMissing(string path)
        {
            //bool folderExists = Directory.Exists(Microsoft.AspNetCore.Server.MapPath(path));
            //if (!folderExists)
            //    Directory.CreateDirectory(Microsoft.AspNetCore.Server.MapPath(path));
        }
    }
}
